name: CI

on:
  push:
  pull_request:

jobs:
  lint:
    name: Strict lint
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
      - run: scripts/lint.sh
